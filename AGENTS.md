# Agent onboarding template

## Project Overview

This is a React SPA for registering PR new sellers. It is essentially a multi-step form.

## Architecture

This project loosely follows a FSD (Feature-Sliced Design) architecture, or React-bulletproof architecture. The main idea is to organize code by features and layers, rather than by technical concerns (like "components", "services", etc.).
